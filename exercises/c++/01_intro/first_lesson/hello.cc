#include <iostream>//command for the preprocessor
//do not forget to include the library that includes the header of th function std

//c++is a static language the type of the symbols as the functions must be known


int main(){
//any indentation is fine
//lets print a message on the standard output //
std::cout << "Hello, world!\n";

std::cout << "Hello, world!" << std::endl;
int t{0};
(void)t;
return 0;
} 
